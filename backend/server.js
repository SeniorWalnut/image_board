const express =    require('express');
const cors 	  =    require('cors');
const pgp = require('pr-promise')(); 

// Epxress middleware
express.use(epxress.json());
express.use(cors());

const server = express();
